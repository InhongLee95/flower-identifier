# DESIGN — 꽃 이름 식별 앱 설계

## 화면 구성 (무엇이 어디에 보이는지)

한 화면(페이지 1개)으로 구성합니다. 데스크톱 브라우저 기준으로 만들고, 화면 폭이 좁아지면(모바일) 아래 영역들을 세로 1열로 쌓는 정도만 최소한으로 대응합니다.

- **상단**: 앱 제목 ("꽃 이름 찾기")
- **가운데 — 업로드 영역**: 사진 선택 버튼, 선택한 사진 미리보기
- **업로드 영역 바로 아래 — 결과 표시 영역**: 상태에 따라 아래 중 하나가 보입니다
  - 분석 중일 때: "분석 중..." 로딩 표시
  - 성공했을 때: 방금 선택한 사진(브라우저에 남아있는 미리보기 objectURL, 서버에 다시 요청하지 않음) + 식별된 꽃 이름 카드
  - 타임아웃/API 오류일 때: "다시 시도해주세요" 안내 + "다시 시도" 버튼
  - 꽃을 인식하지 못했을 때: "꽃을 인식하지 못했습니다" 안내만 (재시도해도 결과가 같으므로 버튼 없음)
- **업로드 영역 안 — 검증 에러 표시**: 파일을 고르는 즉시(서버 전송 전) 형식·용량을 확인해 선택 버튼 아래에 안내 문구를 띄웁니다
  - 형식 오류: "JPG 또는 PNG 파일만 업로드할 수 있어요."
  - 용량 초과: "4MB 이하의 사진만 업로드할 수 있어요." (Vercel 요청 본문 한도(약 4.5MB)를 안전하게 지키기 위해 4MB로 제한)
- **하단 — 식별 기록 영역**: 지금까지 식별한 꽃들을 사진 썸네일 + 이름 + 날짜로 목록 표시 (최근 20개, 스크롤)
  - 기록이 하나도 없으면 "아직 식별한 꽃이 없어요" 같은 안내 문구로 대체
  - 목록 위나 옆에 "전체 삭제" 버튼 배치 (localStorage 기록만 지우며, Supabase Storage의 사진 파일은 삭제하지 않음)

## 데이터 흐름 (입력 → 처리 → 출력)

1. **입력**: 사용자가 꽃 사진 파일을 선택한다
2. **1차 검증(브라우저)**: 형식(JPG/PNG)·용량(4MB) 확인, 어긋나면 업로드 전에 바로 안내하고 중단
3. **업로드**: 이미지를 서버(`/api/identify`)로 전송. 클라이언트는 `AbortController`로 전체 요청에 10초 제한을 걸어, 서버가 응답 없이 멈추는 경우에도 같은 타임아웃 처리로 빠져나온다.
4. **서버 재검증**: 서버에서도 형식·용량을 다시 확인한다 (브라우저 검증 우회 대비). 어긋나면 `INVALID_FILE`로 즉시 응답하고 아래 5·6단계는 진행하지 않는다.
5. **서버 처리(동시 실행)**: 받은 파일을 그대로 두 곳에 병렬로 보낸다 — ⓐ OpenAI(gpt-4o-mini)에 파일 자체(base64)로 전달해 "이 사진 속 꽃 이름이 뭐야?"를 물어보고, ⓑ Supabase Storage(비공개 버킷)에 업로드해 파일 경로를 확보한다. 두 작업을 동시에 실행해 5초 목표를 지킨다. 둘 중 하나라도 실패하면(Storage 업로드 실패 또는 OpenAI 오류) 전체 요청을 실패로 처리한다 — 식별은 됐지만 저장에 실패한 경우도 사용자에게는 동일하게 오류로 안내한다 (부분 성공을 보여주지 않음).
6. **타임아웃**: OpenAI 응답이 10초를 넘기면 요청을 중단하고 타임아웃 오류로 처리한다.
7. **서버 응답**: 꽃 이름 + 이미지 경로(path)를 화면으로 돌려준다. 실패 시에는 오류 종류(`INVALID_FILE`/`TIMEOUT`/`AI_ERROR`/`UPLOAD_FAILED` vs `NOT_A_FLOWER`)를 함께 전달해, 클라이언트가 재시도 버튼을 보여줄지 말지 구분할 수 있게 한다.
8. **클라이언트 처리**: 받은 결과를 화면에 표시하고(방금 선택한 사진의 로컬 미리보기 재사용, 6번 참고), 이미지 경로·꽃 이름·시각(`Date.now()`)을 브라우저 저장소(localStorage)에 기록 추가 (20개 넘으면 오래된 것부터 삭제 — 이때도 Storage의 사진 파일은 그대로 둔다). 버킷이 비공개라 이미지 자체가 아니라 경로만 저장하며, 기록 목록에 과거 항목의 썸네일을 보여줄 때만 `/api/image-url`로 서명 URL을 요청해 불러온다.
9. **출력**: 화면에 꽃 이름이 보이고, 기록 목록에도 새 항목이 즉시 반영됨

## API 스펙 (`/api/identify`)

- 요청: `multipart/form-data`, 필드명 `image`
- 성공 응답: `{ data: { flowerName, imagePath } }`
- 실패 응답: `{ error: { code, message } }` — `code`는 다음 중 하나
  - `NOT_A_FLOWER`: 꽃을 인식하지 못함 (재시도 버튼 숨김)
  - `INVALID_FILE`: 서버 재검증에서 형식/용량 위반 발견 (재시도 버튼 숨김 — 다른 파일을 다시 선택해야 함)
  - `TIMEOUT`: 10초 초과
  - `UPLOAD_FAILED`: Storage 업로드 실패
  - `AI_ERROR`: OpenAI 호출 오류
  - (`TIMEOUT`·`UPLOAD_FAILED`·`AI_ERROR`는 재시도 버튼 표시, `NOT_A_FLOWER`·`INVALID_FILE`은 숨김)

## API 스펙 (`/api/image-url`)

기록 목록(작업 11·12번)에서 비공개 버킷에 저장된 사진의 썸네일을 보여주기 위한 서명 URL 발급 API. 새로고침 시 최대 20개 기록을 한 번에 복원해야 하므로, 경로를 건당이 아니라 배치로 받는다.

- 요청: `GET /api/image-url?paths=path1,path2,...` (콤마로 구분한 이미지 경로 목록, 최대 20개)
- 성공 응답: `{ data: { urls: { [path]: string } } }` — 경로별 임시 서명 URL 맵
- 실패 응답: `{ error: { code: "SIGN_FAILED", message } }`
- 서명 URL 유효 기간: 1시간 (만료 후에는 새로고침 시 다시 발급받는다)

## localStorage 데이터 구조

- 키: `flower-history`
- 값: `{ id, imagePath, flowerName, createdAt }[]` 배열
  - `id`: `crypto.randomUUID()`로 생성
  - `createdAt`: `Date.now()` 값(number)
- 새 항목은 배열 맨 앞에 추가(최신순), 저장할 때마다 `slice(0, 20)`으로 20개 제한
- 기록 썸네일은 `next/image`가 아니라 일반 `<img>` 태그로 표시한다 (서명 URL이 요청마다 바뀌고 만료되는 외부 도메인이라, `next.config.ts`에 원격 이미지 도메인을 등록하지 않고 간단하게 처리하기 위함)

## 기술 선택

기본은 Next.js입니다 — Part 5에서 Vercel로 배포하기로 되어 있어, 화면과 서버 코드를 한 프로젝트 안에서 같이 만들 수 있는 Next.js를 그대로 씁니다.

그 위에 이 앱이 "사진을 이해하고 저장"해야 해서 아래 3가지가 추가로 필요합니다.

| 기술 | 쉬운 설명 (1줄) |
|---|---|
| **Tailwind CSS** | 버튼 색깔, 글자 크기, 화면 배치 같은 디자인을 빠르게 입혀주는 도구예요. |
| **OpenAI (gpt-4o-mini)** | 사진을 보여주면 "이건 이런 꽃이에요"라고 설명해주는 AI예요. |
| **Supabase Storage** | 업로드한 사진 파일을 인터넷 어딘가에 안전하게 보관해주는 저장 창고예요. |

OpenAI(`OPENAI_API_KEY`)는 `.env`에 이미 준비된 키로 바로 연결합니다. Supabase는 키가 두 종류로 나뉩니다 — `SUPABASE_ACCESS_TOKEN`은 CLI로 프로젝트·버킷을 만들 때만 쓰는 관리용 키이고, 앱이 실제로 사진을 올리고 불러올 때 쓰는 `SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY`는 버킷을 만드는 시점에 별도로 발급받아 `.env`에 추가합니다.
