# PLAN — 꽃 이름 식별 앱 개발 계획

## 이번 사이클 목표
PRD.md에 정의된 두 핵심 기능(꽃 사진 업로드 및 식별, 식별 기록 확인)을 모두 동작하는 상태로 구현하고 Vercel에 배포한다.

## 성공 기준
- 사진을 업로드하면 5초 이내에 꽃 이름 식별 결과가 표시된다
- 응답이 10초를 넘기면 타임아웃 처리되어 "다시 시도해주세요" 안내와 재시도 버튼이 뜬다
- 꽃이 아니거나 식별 불가한 사진은 "꽃을 인식하지 못했습니다" 안내가 뜬다 (재시도 버튼 없음)
- 식별 기록이 localStorage에 남고, 새로고침해도 사라지지 않으며, 최근 20건까지 유지된다 (초과 시 오래된 것부터 삭제)
- 기록이 없을 때 빈 상태 문구가 뜨고, "전체 삭제" 버튼이 정상 동작한다
- Vercel에 배포되어 실제 URL로 접속·사용 가능하다

## 작업 목록 (만들 순서대로)

1. Next.js(App Router) 프로젝트 초기화
2. Tailwind CSS 설치 및 기본 스타일 설정
3. 업로드 화면 기본 레이아웃(업로드 영역 + 결과 표시 영역) 뼈대 구성
4. 이미지 업로드 입력(파일 선택, JPG/PNG·4MB 제한 — Vercel 요청 본문 한도 대응) 구현
5. Supabase 프로젝트/비공개 버킷 연결 — `SUPABASE_URL`·`SUPABASE_SERVICE_ROLE_KEY` 발급 후 `.env`에 추가 — 및 이미지 업로드 기능 구현
6. OpenAI API(gpt-4o-mini, 이미지 인식) 연동용 서버 API 라우트(/api/identify) 구현 (서버 재검증·병렬 실패 처리 포함)
7. 업로드 → (Storage 저장 + AI 식별을 동시 실행) → 결과 반환까지 전체 흐름 연결
8. 식별 결과(꽃 이름)를 화면에 표시하는 기능 구현
9. 식별 중 로딩 상태 표시, 10초 타임아웃 처리(타임아웃/API 오류 시 재시도 버튼), 꽃 인식 실패 시 버튼 없는 안내 메시지 구현
10. 식별 결과(이미지 경로·꽃 이름·날짜)를 localStorage에 저장하는 기능 구현 (최근 20개 유지, 초과 시 오래된 것부터 삭제 — Storage의 사진 파일은 그대로 둠)
11. 기록 경로들의 서명 URL을 한 번에 발급하는 서버 API 라우트(/api/image-url) 구현 (비공개 버킷 썸네일용, 최대 20개 배치 요청)
12. 식별 기록 목록 UI 및 빈 상태 안내 문구 구현 (/api/image-url로 썸네일 표시)
13. 새로고침 시 저장된 식별 기록을 불러와 화면에 복원하는 기능 구현
14. "전체 삭제" 버튼 기능 구현 (localStorage 목록만 비우며, Storage의 사진 파일은 삭제하지 않음)
15. 전체 흐름(업로드 → 식별 → 기록 확인) 수동 테스트 및 버그 수정 (데스크톱 기준 + 모바일 폭 반응형 최소 확인 포함)
16. Vercel 배포 설정, 환경변수(.env) 등록, 실제 배포 확인
