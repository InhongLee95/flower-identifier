# CHECK — 배포 전 종합 점검

## 판정: 실패 (FAIL)

Gap 13건 · 확인 필요 2건 · 보안 조치 필요 4건이 아직 남아 있어 배포 보류를 권장합니다. (Critical 수준 보안 위험은 이미 해소됨 — 아래 참고)

---

## 1. 성공 기준 판정 (PLAN.md)

| 기준 | 판정 | 사유 |
|---|---|---|
| 5초 이내 결과 표시 | ✅ 통과 | 실제 이미지로 측정, 2.245초 |
| 10초 타임아웃 → 재시도 버튼 | ⏳ 미확인 | 실제 재현 테스트 아직 못 함 |
| 꽃 인식 실패 → 안내만(버튼 없음) | ✅ 통과 | curl 테스트로 확인 |
| localStorage 20건 유지·새로고침 후 유지 | ✅ 통과 | 22건 시뮬레이션 후 새로고침 → 정확히 20건 확인 |
| 빈 상태 문구 + 전체 삭제 버튼 동작 | ✅ 통과 | 클릭 시 UI·localStorage 모두 초기화 확인 |
| Vercel 배포 후 실제 URL 접속 | ❌ 미착수 | PLAN 16번, 진행 전 확인 대기 중 |

---

## 2. Gap 목록 (DESIGN.md 설계 vs 실제 구현)

지난 Gap 분석 14건 중 1건(서버 파일 재검증에 매직 넘버 확인 추가)은 이후 보안 점검 때 같이 고쳐져서 **해소됨**. 나머지 13건은 그대로 남아 있습니다.

## 3. 보안 점검 결과

1~3번(높은 위험: API 무제한 호출, 비공개 버킷 경로 노출, 파일 형식 스푸핑)과 이후 추가로 지적된 Origin 미검증·서버 로그 없음·보안 헤더 없음·rate limit 메모리 누적·버킷 크기 제한 없음은 **모두 코드/설정으로 조치 완료**하고 재검증까지 마쳤습니다. 아래 "고쳐야 할 항목"의 보안 관련 잔여 항목은 계정 설정이 필요해 제가 대신 할 수 없는 것과, 사용자가 명시적으로 보류하기로 한 것뿐입니다.

---

## 고쳐야 할 항목 (우선순위 순)

### 🔴 최우선 — 실사용 버그 또는 배포 전 필수
1. **OpenAI 월 사용 한도 미설정** — 계정 설정 필요(제가 대신 못 함), 안 걸면 무제한 과금 위험
2. **검증 실패 후 "다시 시도"가 이전 파일을 재식별함** — `app/page.tsx`, `handleFileChange`가 실패 시 `result`·`lastFileRef`를 안 지워서 발생하는 실제 오작동
3. **`formData()` 파싱이 try 블록 밖** — `app/api/identify/route.ts:45`, 손상된 요청 시 정의된 에러 응답 형식이 깨지고 기본 500이 나감
4. **Storage 업로드가 예외로 실패하면 `AI_ERROR`로 잘못 분류됨** — `uploadResult.error` 반환 경로만 `UPLOAD_FAILED` 처리, throw는 못 잡음
5. **타임아웃 시 Storage 업로드가 계속 진행돼 고아 파일 축적** — 버킷 크기·형식 제한은 걸어뒀지만(부분 완화) 정리 정책은 없음

### 🟠 중요 — 설계 불일치 / 안정성
6. **업로드 영역에 "선택한 사진 미리보기"가 없음** — DESIGN.md 명시 요소가 화면에 없어, 분석 중(최대 10초) 뭘 올렸는지 안 보임
7. **서버·클라이언트 타임아웃이 둘 다 10초** — 클라이언트가 항상 먼저 끊어서 서버의 `TIMEOUT` 응답 경로가 사실상 죽어있음
8. **`/api/image-url` 실패가 조용히 무시됨** — `page.tsx:40`의 `.catch(() => {})`, 썸네일이 안 떠도 사용자에게 안내 없음
9. **Vercel 배포 시 `.env` 6개 키 중 실제 쓰는 3개(`OPENAI_API_KEY`/`SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`)만 등록해야 함** — 아직 배포 전이라 실행 안 함, 배포 시점에 반드시 챙길 것

### 🟡 낮음 — 사소하지만 남아있음
10. `loadHistory`가 저장된 데이터 형태를 검증하지 않음 (손상 시 렌더 오류 가능)
11. 클라이언트가 모든 fetch 실패 원인을 `TIMEOUT` 하나로 뭉뚱그려 표시함
12. 다크모드에서 글자 대비가 낮아 잘 안 보임 (`globals.css` 잔재)
13. `URL.createObjectURL` 해제 안 해 식별할 때마다 메모리 누수
14. 기록 1건 추가될 때마다 최대 20개 서명 URL을 매번 전량 재발급 (비효율)

### ⏳ 확인 필요
15. PLAN 성공 기준 "10초 타임아웃 → 재시도 버튼" 실제 재현 테스트 아직 안 함
16. PLAN 16번(Vercel 배포) 미착수

---

## 보류 (사용자가 지금은 안 고치기로 결정 — 액션 아이템 아님)
- 프롬프트 인젝션 방어 없음 (사진 속 텍스트로 AI 응답 조작 가능, 실제 피해는 "이상한 이름이 뜨는" 수준)
- 사진 EXIF(위치정보 등)가 그대로 저장·전송됨 (단일 사용자 도구라 볼 사람은 본인뿐)
