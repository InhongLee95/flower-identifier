# 꽃 이름 찾기 서비스 기획서 (PRD)

- 작성자: [이인홍]
- 작성일: (260813)

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

저(개발자 본인)는 산책이나 여행 중 예쁜 꽃을 보면 이름이 궁금할 때가 많습니다. 매번 포털에서 비슷한 사진을 찾아 비교하며 검색하는 과정이 번거로워, 사진만 올리면 바로 꽃 이름을 알려주는 개인용 앱이 필요합니다.

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

현재는 꽃 사진을 찍은 뒤 포털 이미지 검색이나 식물 도감 앱을 따로 찾아 비교해야 합니다. 정확한 이름을 확인하기까지 여러 사이트를 오가야 하고 시간이 걸립니다.

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

- 사진 업로드 후 결과 표시까지 5초 이내
- 꽃 이름 확인까지 걸리는 시간을 기존 검색 방식(평균 2~3분) 대비 30초 이내로 단축
- 최근 식별 기록 20건까지 화면에서 즉시 재확인 가능

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

사용자(개발자 본인) → 앱 접속 → 꽃 사진 업로드 → AI 분석 결과(꽃 이름) 확인 → (필요 시) 이전 식별 기록 스크롤하여 재확인

**예외 상황**
- API 오류·네트워크 끊김·응답 지연 → 일정 시간(10초) 초과 시 타임아웃 처리, 안내 메시지와 함께 재시도 버튼 제공
- 꽃이 아니거나 식별 불가한 사진 → "꽃을 인식하지 못했습니다" 안내만 표시 (재시도해도 결과가 같으므로 재시도 버튼 없음, 5번 참고)

## 5. 주요 기능 (must / nice로 구분)

**Must**

1) 꽃 사진 업로드 및 식별 기능
- 설명: 사용자가 꽃 사진을 업로드하면 AI(OpenAI gpt-4o-mini의 이미지 인식 기능)가 사진을 분석해 꽃 이름을 알려줍니다. 이미지는 서버가 받아 OpenAI에는 파일 자체(base64)로 직접 전달하고, 저장용으로 Supabase Storage에도 함께 올립니다 (Storage 업로드와 AI 식별은 동시에 진행해 5초 목표를 지킵니다).
- AI가 꼭 지켜야 할 규칙:
  - 업로드 후 결과 표시까지 5초 이내
  - 지원 이미지 형식은 JPG/PNG, 최대 4MB로 제한 (Vercel의 요청 본문 한도(약 4.5MB)를 안전하게 지키기 위해 5MB에서 하향 조정)
  - 형식이 틀리면 "JPG 또는 PNG 파일만 업로드할 수 있어요.", 용량 초과 시 "4MB 이하의 사진만 업로드할 수 있어요."를 업로드 전에 바로 표시할 것
  - 응답이 10초를 넘기면 타임아웃 처리하고 "다시 시도해주세요" 안내와 재시도 버튼을 표시할 것 (오류/타임아웃 공통 문구)
  - 꽃이 아닌 사진이거나 식별 불가 시에는 "꽃을 인식하지 못했습니다" 안내만 표시할 것 (재시도해도 결과가 같으므로 재시도 버튼은 넣지 않음)

2) 식별 기록 확인 기능
- 설명: 이전에 업로드한 사진과 식별된 꽃 이름을 목록으로 다시 볼 수 있습니다. 사진은 Supabase Storage(비공개 버킷)에 저장하고, 기록 목록(이미지 경로·꽃 이름·날짜)은 브라우저 로컬 저장(localStorage)에 보관합니다. 버킷이 비공개이므로 화면에 썸네일을 보여줄 때는 저장된 경로로 임시 서명 URL(signed URL)을 새로 발급받아 불러옵니다. 날짜는 결과를 받은 시점에 브라우저에서 기록합니다.
- AI가 꼭 지켜야 할 규칙:
  - 최근 기록은 최대 20개까지 유지하며, 20개를 초과하면 가장 오래된 기록부터 자동 삭제할 것 (Storage에 남은 사진 파일 자체는 이번 범위에서는 지우지 않음 — 6번 비범위 참고)
  - 새로고침해도 기록이 사라지지 않을 것
  - 기록이 하나도 없을 때는 빈 상태 안내 문구를 표시할 것
  - 저장된 기록을 한 번에 지울 수 있는 "전체 삭제" 버튼을 제공할 것 (목록만 비우며, Storage의 사진 파일은 지우지 않음)

**Nice to have**

- 이번 범위에는 없음

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

- 범위 (만들 것)
  - 꽃 사진 업로드 및 식별 기능
  - 식별 기록 확인 기능
- 비범위 (만들지 않을 것)
  - 회원가입/로그인 기능 (나 혼자 쓰는 앱이므로 불필요)
  - 여러 꽃 비교/식물 도감 기능
  - 위치 기반 꽃 정보(지도 연동)
  - 다국어 지원
  - 모바일 네이티브 앱 (웹으로만 사용, 데스크톱 브라우저 우선 제작·모바일은 반응형으로 최소한만 대응)
  - Storage에 쌓인 사진 파일 자동 정리 (기록이 삭제되어도 Supabase에 저장된 실제 사진 파일은 지우지 않음)

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

- 공개등급: 비공개 (개인용 문서, 외부 공유 없음). Supabase Storage 버킷도 비공개로 설정하며, OpenAI에는 이미지 URL이 아닌 파일 자체(base64)를 전달하므로 버킷을 공개로 열 필요가 없습니다.
- 개인정보 여부: 사용자 본인만 이용하며 별도의 회원 정보를 수집하지 않습니다. 다만 업로드하는 사진 자체가 개인이 촬영한 이미지일 수 있어 위치·인물 등이 함께 찍히지 않도록 주의가 필요하며, 이미지는 분석을 위해 OpenAI API로 전송되고 보관을 위해 Supabase Storage에 저장됩니다.
- API 키 관리: `.env`의 `GITHUB_TOKEN`, `SUPABASE_ACCESS_TOKEN`, `VERCEL_TOKEN`, `OPENAI_API_KEY`는 `.gitignore`에 등록되어 저장소(GitHub)에 노출되지 않도록 관리합니다. 이 중 `SUPABASE_ACCESS_TOKEN`은 Supabase CLI로 프로젝트·버킷을 만들 때 쓰는 관리용 토큰이며, 앱이 실제로 사진을 올리고 받아올 때 쓰는 프로젝트 URL과 서비스 키(예: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`)는 버킷 생성 시점에 별도로 발급받아 `.env`에 추가합니다.

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- Next.js (App Router) — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- OpenAI gpt-4o-mini — 이미지 인식 기반 꽃 식별용 모델
- Tailwind CSS — 화면 스타일링
- Supabase Storage — 업로드된 꽃 사진 저장
- 배포: Vercel 기본 제공 서브도메인 사용 (커스텀 도메인은 이번 범위 아님)
- 고정 이유: Part 1~2 실습에서 쓴 것과 같은 구성이고, Part 5에서 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함

## 개발 단위

세부 작업 순서는 `PLAN.md`를 참고한다 (이 문서의 5번·6번 요구사항을 기준으로 작성됨, 두 곳에서 중복 관리하지 않기 위해 여기서는 목록을 따로 두지 않음).
